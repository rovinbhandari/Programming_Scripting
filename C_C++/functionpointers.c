/*
 * "In C, a function itself is not a variable, but it is possible to
 * define pointers to functions, which can be assigned, placed in
 * arrays, passed to functions, returned by functions, and so on."
 *
 * -K&R
 */

#include <stdio.h>

// passing function pointers to another function




/*********************************************************************************************************************************************************************/

// making an array of function pointers and passing a pointer to that array to another function for condition based selection of function(s) from the array


