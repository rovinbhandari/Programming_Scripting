maintain an array of pairs <x, y>.
x-sort the array.
iteratively, using binary search, find the first index for which the end time of the current job is less than the start time of any job.
				subtract that index with the index of the current job to find the total overlapping jobs.

O(nlonn)
